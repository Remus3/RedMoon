API-Key-Claude.txt
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.superpowers/
_scratch/
# Redundant with _scratch/ above, kept because it is where the .NET build is
# deliberately redirected (spec D8) and that is worth stating out loud.
_scratch/bridge-build/
# Belt and braces, for a developer who builds without Directory.Build.props.
bridge/**/bin/
bridge/**/obj/
logs/
ops/runtime/
data/rmdata/

# The RC-to-sibling handoff channel. Notes arrive here from other projects on
# this machine and are never Red Moon source. tests/test_ports.py additionally
# excludes it from the port scan, which .gitignore alone does not do.
moon_sync_inbox/
